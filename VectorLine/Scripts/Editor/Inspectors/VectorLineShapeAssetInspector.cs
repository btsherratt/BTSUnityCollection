/*using UnityEditor;
using UnityEngine;

[CustomEditor(typeof(VectorLineShapeAsset<>), true)]
public class VectorLineShapeAssetInspector : Editor {
    public override void OnInspectorGUI() {
        serializedObject.Update();

        SerializedProperty property = serializedObject.FindProperty("m_shape");
        EditorGUILayout.PropertyField(property);

        serializedObject.ApplyModifiedProperties();
    }
}*/
